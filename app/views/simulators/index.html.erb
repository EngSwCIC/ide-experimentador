<!DOCTYPE html>
<html>
<head>
  <title>Configuração do Ambiente</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>

<h1>Configuração do Ambiente</h1>
<br>



<p>
  <%= link_to "Criar Simulador", new_simulator_path, class: 'btn btn-primary' %>
</p>

<h2>Simuladores:</h2>

<% @simulators.each do |simulator| %>
  <div class="card" style="width: 18rem;">
    <div class="card-body">
      <h5 class="card-title"><%= simulator.name %></h5>
      <a id='ver-experimentos' href="/simulators/<%= simulator.id %>">Ver Experimentos</a>
      <% if simulator.disabled %>
        <p class="button"><%= link_to "Desativado", toggle_simulator_path(simulator), method: :put %></p>
      <% else %>
        <p class="button"><%= link_to "Ativado", toggle_simulator_path(simulator), method: :put %></p>
      <% end %>
      <%= link_to "Remover", delete_simulator_path(simulator), method: :delete, 
      data: {turbo_confirm: "Você tem certeza?"}%>
    </div>
  </div>
<% end %>

</body>
</html>
